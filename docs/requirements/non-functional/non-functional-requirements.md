# 非機能要件定義書

**バージョン**: 1.0.0  
**作成日**: 2026-05-09  
**ステータス**: Draft

---

## 1. 非機能要件一覧

| 項目 | 要件 |
|------|------|
| **認証セキュリティ** | パスワードはbcrypt等でハッシュ化して保存 |
| **認可** | サーバーサイドでロールチェックを実施（フロントのみの制御は不可） |
| **レスポンス** | 一般的な操作で3秒以内のレスポンス |
| **対応ブラウザ** | Chrome / Edge 最新版 |
| **言語** | 日本語 |
| **監査ログ** | マスタ変更・ユーザー管理操作は更新者・更新日時を記録 |

---

## 2. セキュリティ要件詳細

### 2.1 認証

- メールアドレス＋パスワードによるログイン
- パスワードはbcryptでハッシュ化して保存（平文保存禁止）
- 初期パスワードはシステムが自動生成（推測されにくいランダム文字列）
- 初回ログイン時にパスワード変更を強制する
- セッション管理はJWTを使用する

### 2.2 認可

- ロール（一般 / TL / 管理者）に基づくアクセス制御
- Spring Security によりサーバーサイドで権限チェックを実施
- フロントエンドのみの表示制御は補助的なものとし、APIレベルで必ず制御する

### 2.3 パスワードポリシー

- 初期パスワード：システム自動生成（英大文字・小文字・数字・記号を含む12文字以上）
- 初期パスワードは登録時に管理者画面へ一度だけ表示し、以後は参照不可とする

---

## 3. 性能要件

- 通常操作（一覧表示・入力保存）：3秒以内のレスポンス
- 同時接続ユーザー数の上限はTBD（社員規模に依存）

---

## 4. 可用性・運用要件

- ホスティング：AWS EC2
- データベース：AWS RDS（PostgreSQL 16.4）
- バックアップ：RDSの自動バックアップを利用（設定はインフラ設計で定義）
- ログ：アプリケーションログをEC2上に出力

---

## 5. 監査ログ要件

以下の操作について、操作者・操作日時を記録する。

| 操作種別 | 対象 |
|---------|------|
| 作成・更新・削除 | ITスキルマスタ、参考資格マスタ、ADマスタ、分類マスタ、レベルマスタ |
| 作成・更新・削除 | ユーザー、年度マスタ |
| カスタムスキル昇格 | ITスキル、資格 |

---

## 6. 将来機能（第2次開発候補）

> **注意**: このセクションに記載された機能は**現時点では実装予定がない**。  
> 将来的に実装したい内容の記録であり、優先度・スコープ・スケジュールはすべて未定。

---

### 6.1 スキル可視化

棚卸結果をグラフ・チャートで視覚的に把握できるようにする。

| 機能 | 概要 |
|------|------|
| **レーダーチャート** | ユーザーのITスキルレベルをカテゴリ1別にレーダーチャートで表示 |
| **スキルマップ** | チーム単位でメンバーのスキル分布を一覧表示 |
| **年度推移グラフ** | スキルレベルの年度ごとの変化を折れ線グラフ等で可視化 |
| **カバレッジ分析** | 分類別・チーム全体のスキルカバレッジ率を集計・表示 |

**実装時の考慮事項**

- フロントエンドのグラフライブラリとして Recharts または Chart.js の採用を想定
- 可視化用のデータ集計APIを追加で設計・実装する必要がある
- 既存のテーブル設計（`it_skill_details`、`it_skill_categories` の階層構造）はレーダーチャートの軸（カテゴリ1）とのマッピングがそのまま利用可能

---

### 6.2 データエクスポート / レポート

棚卸結果を社内の他システムや分析ツールで活用できるよう、出力機能を提供する。

| 機能 | 概要 |
|------|------|
| **CSV / Excel 出力** | ユーザー・年度を指定して棚卸データをエクスポート |
| **チーム集計レポート** | チーム単位のスキルレベル平均・分布を一覧出力 |
| **全社集計レポート** | 管理者向けに全社のスキル状況を集計・出力 |

---

### 6.3 TL面談記録

年度末にスキル棚卸結果をもとにTLと社員が面談を実施する。その面談結果をシステム上で記録・管理する画面を提供する。

| 機能 | 概要 |
|------|------|
| **面談記録入力** | TLが面談コメント・評価・来期目標などを入力・保存 |
| **面談記録参照** | 社員が自分の面談記録を参照 |
| **面談記録一覧** | TL・管理者がチームの面談記録を一覧で確認 |

**実装時の考慮事項**

- 面談記録は棚卸データ（`inventories`）に紐付ける設計が自然
- 面談記録テーブルの追加が必要（`interview_records` 等）
- 閲覧権限は本人・担当TL・管理者に限定する
